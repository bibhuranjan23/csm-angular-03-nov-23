import { AppAlterBackgroundHandlerDirective } from './app-alter-background-handler.directive';

describe('AppAlterBackgroundHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new AppAlterBackgroundHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
